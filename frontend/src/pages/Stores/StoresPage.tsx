import React from 'react';
import { Container, Typography } from '@mui/material';

const StoresPage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Shopify Stores</Typography>
      <Typography>Manage your connected Shopify stores</Typography>
    </Container>
  );
};

export default StoresPage;